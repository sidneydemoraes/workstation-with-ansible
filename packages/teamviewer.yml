- name: Download TeamViewer installer
  get-url: url={{ teamviewer_url }} dest={{ tmp_dir }}

- name: Install TeamViewer
  become: yes
  apt: deb={{ teamviewer_file }}

- name: Remove .deb file
  file:
    path: "{{ teaviewer_file }}"
    state: absent

