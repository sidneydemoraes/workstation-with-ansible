- name: "Add Slimbook Repository"
  become: true
  apt_repository: 
    repo: ppa:libreoffice/ppa
    state: present
    update_cache: true
    validate_certs: false

- name: "Install Slimbook"
  become: true
  apt:
    name: slimbookbattery
    state: present
